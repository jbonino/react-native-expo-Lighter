import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textTitle:{
    fontSize: 20,
  },
  center: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
});

const primaryColor = '#1B98E0';

export default styles;